# 系统架构说明

## 🏗️ 完整架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         你的本地电脑                              │
│                                                                 │
│  ┌─────────────────┐                                            │
│  │ Claude Desktop  │                                            │
│  │                 │                                            │
│  │  "请检查v885    │                                            │
│  │   的资源完整性" │                                            │
│  └────────┬────────┘                                            │
│           │ MCP协议                                             │
│           ↓                                                     │
│  ┌─────────────────────────┐                                   │
│  │ MCP Server (Remote)     │                                   │
│  │ mcp-server-remote.js    │                                   │
│  │                         │                                   │
│  │ - 接收MCP工具调用       │                                   │
│  │ - 创建HTTP请求          │                                   │
│  │ - 连接SSE流             │                                   │
│  │ - 实时接收输出          │                                   │
│  │ - 返回结果给Claude      │                                   │
│  └────────┬────────────────┘                                   │
│           │ HTTP + SSE                                          │
└───────────┼─────────────────────────────────────────────────────┘
            │
            │ Internet / VPN
            │
┌───────────┼─────────────────────────────────────────────────────┐
│           ↓                                                     │
│  ┌─────────────────────────┐         远程服务器                │
│  │ SSE API Server          │                                   │
│  │ api-server-sse.js       │                                   │
│  │ (PM2管理，端口3000)     │                                   │
│  │                         │                                   │
│  │ 安全检查：              │                                   │
│  │ ├─ IP白名单             │                                   │
│  │ ├─ API Key认证          │                                   │
│  │ └─ 速率限制             │                                   │
│  │                         │                                   │
│  │ 任务管理：              │                                   │
│  │ ├─ 创建任务             │                                   │
│  │ ├─ 异步执行             │                                   │
│  │ ├─ SSE流式推送          │                                   │
│  │ └─ 进度跟踪             │                                   │
│  └────────┬────────────────┘                                   │
│           │                                                     │
│           ↓                                                     │
│  ┌─────────────────────────┐                                   │
│  │ Task Executor           │                                   │
│  │                         │                                   │
│  │ - spawn执行shell命令    │                                   │
│  │ - 实时捕获stdout/stderr │                                   │
│  │ - 推送到任务管理器      │                                   │
│  └────────┬────────────────┘                                   │
│           │                                                     │
│           ↓                                                     │
│  ┌──────────────────────────────────────┐                      │
│  │ Shell Scripts (实际执行的脚本)       │                      │
│  │                                      │                      │
│  │ - /home/ec2-user/match/match         │                      │
│  │ - /export/nginx/https/pubfbclient.sh │                      │
│  │ - /export/nginx/https/pubclient.sh   │                      │
│  │ - mv commands                        │                      │
│  └──────────────────────────────────────┘                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 使用方式对比

### 方式1️⃣：MCP远程（推荐）⭐⭐⭐⭐⭐

```
你 → Claude Desktop → 本地MCP服务器 → 远程API → 执行脚本
```

**优点：**
- ✅ 最佳用户体验（在Claude Desktop中直接对话）
- ✅ 支持长时间任务
- ✅ 实时输出
- ✅ 不需要写命令

**缺点：**
- ⚠️ 需要配置MCP
- ⚠️ 需要网络连接

**适合：** 日常使用，AI辅助操作

---

### 方式2️⃣：Node.js脚本 ⭐⭐⭐⭐

```
你 → 运行脚本 → 远程API → 执行脚本
```

**命令：**
```bash
node scripts/run-task.js check-integrity v885
```

**优点：**
- ✅ 简单直接
- ✅ 支持长时间任务
- ✅ 实时输出
- ✅ 不需要配置MCP

**缺点：**
- ⚠️ 需要Node.js环境
- ⚠️ 需要手动输入命令

**适合：** 脚本自动化，CI/CD

---

### 方式3️⃣：Bash脚本 ⭐⭐⭐

```
你 → 运行脚本 → 远程API（轮询） → 执行脚本
```

**命令：**
```bash
./scripts/run-task.sh check-integrity v885
```

**优点：**
- ✅ 不需要Node.js
- ✅ 支持长时间任务
- ✅ 纯Bash，兼容性好

**缺点：**
- ⚠️ 轮询方式，延迟2秒
- ⚠️ 日志显示较少

**适合：** 老系统，无Node.js环境

---

### 方式4️⃣：标准API ⭐⭐

```
你 → curl命令 → 远程API → 执行脚本（等待完成）
```

**命令：**
```bash
curl -X POST http://server:3000/api/check-integrity \
  -H "Content-Type: application/json" \
  -H "X-API-Key: xxx" \
  -d '{"version": "v885"}'
```

**优点：**
- ✅ 最简单（一行curl）
- ✅ 不需要任何依赖

**缺点：**
- ❌ 长时间任务会超时
- ❌ 看不到实时输出

**适合：** 快速测试，短时间任务

---

## 🎯 使用场景推荐

| 场景 | 推荐方式 | 原因 |
|------|---------|------|
| **日常使用** | MCP远程 | 最方便，在Claude Desktop中对话即可 |
| **自动化脚本** | Node.js脚本 | 可编程，实时输出 |
| **CI/CD集成** | Node.js脚本 | 返回值明确，易于集成 |
| **老服务器** | Bash脚本 | 不需要Node.js |
| **快速测试** | 标准API | 最简单，一行命令 |

---

## 🔄 数据流详解

### MCP远程调用流程

```
1. 用户输入
   "请检查v885的资源完整性"
   ↓

2. Claude Desktop
   识别需要调用 check_resource_integrity_remote 工具
   ↓

3. 本地MCP服务器
   接收工具调用请求
   {
     tool: "check_resource_integrity_remote",
     arguments: { version: "v885" }
   }
   ↓

4. 创建远程任务
   POST http://your-server:3000/api/tasks/check-integrity
   Body: { "version": "v885" }
   ↓

5. 远程API服务器
   创建任务，返回taskId
   {
     taskId: "123e4567-...",
     streamUrl: "/api/tasks/123e4567-.../stream"
   }
   ↓

6. 连接SSE流
   GET http://your-server:3000/api/tasks/123e4567-.../stream
   ↓

7. 远程服务器执行脚本
   spawn("/bin/bash", ["-c", "cd /home/ec2-user/match && ./match ..."])
   ↓

8. 实时推送输出（SSE事件）
   data: {"type":"log","log":{"message":"Checking iOS manifest..."}}
   data: {"type":"log","log":{"message":"All files matched..."}}
   data: {"type":"update","task":{"progress":50}}
   ...
   ↓

9. 本地MCP服务器接收
   收集所有日志输出
   格式化为文本
   ↓

10. 返回给Claude Desktop
    "📡 Connected to remote task stream
     🚀 Task started...
     📋 [15:30:45] Checking iOS manifest...
     ✅ All files matched successfully.
     ..."
    ↓

11. Claude回复用户
    "检查完成！所有资源正常。
    - iOS资源：✅ 完整 (1245个文件)
    - Android资源：✅ 完整 (1387个文件)"
```

---

## 🔐 安全层次

```
┌────────────────────────────────────────┐
│ 第1层：网络安全                         │
│ - 防火墙                               │
│ - VPN / SSH隧道                        │
│ - HTTPS (通过Nginx)                    │
└────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────┐
│ 第2层：应用层安全                       │
│ - IP白名单 (ALLOWED_IPS)               │
│ - API Key认证 (X-API-Key header)       │
│ - 速率限制 (100 req/15min)             │
└────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────┐
│ 第3层：系统权限                         │
│ - PM2进程隔离                          │
│ - 文件系统权限                         │
│ - 命令执行权限                         │
└────────────────────────────────────────┘
```

---

## 💾 存储和日志

```
远程服务器：
/home/ec2-user/slots-resource-sync/
├── logs/
│   ├── combined.log          # 所有日志
│   ├── error.log             # 错误日志
│   ├── pm2-out-sse.log       # PM2 stdout
│   └── pm2-error-sse.log     # PM2 stderr
│
├── src/
│   ├── api-server-sse.js     # SSE API服务器
│   ├── tasks/index.js        # 任务执行逻辑
│   └── utils/
│       ├── task-manager.js   # 任务管理器（内存）
│       └── task-executor.js  # 命令执行器
│
└── .env                      # 环境配置

本地电脑：
/本地路径/slots-resource-sync/
├── src/
│   └── mcp-server-remote.js  # MCP服务器（本地运行）
│
├── scripts/
│   ├── run-task.js          # Node.js客户端
│   └── run-task.sh          # Bash客户端
│
└── examples/
    └── sse-client-example.html  # 浏览器客户端
```

---

## 🚀 性能特点

| 指标 | 标准API | SSE API |
|------|--------|---------|
| **响应时间** | 等待任务完成（分钟级） | 立即返回taskId (<100ms) |
| **并发支持** | 受超时限制 | 支持多任务并发 |
| **内存占用** | 低 | 中等（每个任务保留日志） |
| **网络连接** | 短连接 | 长连接（SSE） |
| **客户端复杂度** | 简单（curl） | 中等（需SSE支持） |

---

## 📈 扩展性

当前系统设计支持：

- ✅ **水平扩展**：可以部署多个API服务器，使用负载均衡
- ✅ **任务队列**：可以集成Redis或消息队列
- ✅ **分布式任务**：可以将任务分发到不同的服务器
- ✅ **监控告警**：日志系统完善，易于集成监控

未来可能的增强：

- 🔄 任务持久化（目前只保留在内存中）
- 🔄 WebSocket支持（双向通信）
- 🔄 任务优先级和队列
- 🔄 多用户隔离

---

## 🎓 总结

**这个系统的核心价值：**

1. **解决超时问题**：长时间任务通过异步+SSE流式输出，永不超时
2. **提升用户体验**：实时看到脚本输出，了解执行进度
3. **灵活的使用方式**：支持MCP、脚本、API等多种调用方式
4. **完善的安全机制**：多层安全防护，适合生产环境

**架构特点：**

- 📡 **异步架构**：任务立即创建，后台执行
- 🔄 **实时流式**：SSE推送，毫秒级延迟
- 🛡️ **多层安全**：网络+应用+系统三层防护
- 🎯 **多种客户端**：MCP、Node.js、Bash、浏览器

**适用场景：**

- ✅ 长时间运行的构建/部署任务
- ✅ 需要实时反馈的运维操作
- ✅ 在AI工具中集成的自动化流程
- ✅ 远程服务器管理

**推荐配置：**

```
日常使用：MCP远程 + Claude Desktop
自动化：  Node.js脚本 + CI/CD
紧急操作：标准API + curl
```
