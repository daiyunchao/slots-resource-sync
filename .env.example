# API服务器配置
API_PORT=3000

# API密钥（必须设置，用于认证）
# 生成方法: node scripts/generate-api-key.js
# 这是一个固定的Key，生成一次即可长期使用（每次请求都用这个Key）
# 示例: API_KEY=Kx7mN2pQ9vRs4Yt8
API_KEY=

# IP白名单（逗号分隔，留空则允许所有IP）
# 示例: ALLOWED_IPS=192.168.1.100,10.0.0.50
# 允许所有: ALLOWED_IPS=*
ALLOWED_IPS=

# CORS允许的源（默认允许所有）
CORS_ORIGIN=*

# 代理信任设置（用于反向代理场景，如Nginx）
# - true: 信任所有代理（推荐，适用于大多数场景）
# - false: 不信任代理（仅当直接访问时使用）
# - number: 信任指定数量的代理跳数（如 1 表示信任第一层代理）
TRUST_PROXY=true

# 日志级别: error, warn, info, debug
LOG_LEVEL=info

# 运行环境: development, production
NODE_ENV=production
