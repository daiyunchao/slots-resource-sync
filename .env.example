# API服务器配置
API_PORT=3000

# API密钥（必须设置，用于认证）
# 生成方法: node scripts/generate-api-key.js
# 这是一个固定的Key，生成一次即可长期使用（每次请求都用这个Key）
# 示例: API_KEY=Kx7mN2pQ9vRs4Yt8
API_KEY=

# IP白名单（逗号分隔，留空则允许所有IP）
# 示例: ALLOWED_IPS=192.168.1.100,10.0.0.50
# 允许所有: ALLOWED_IPS=*
ALLOWED_IPS=

# CORS允许的源（默认允许所有）
CORS_ORIGIN=*

# 日志级别: error, warn, info, debug
LOG_LEVEL=info

# 运行环境: development, production
NODE_ENV=production
